kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx  # This must match the Pod label
  ports:
    - protocol: TCP
      port: 80         # Port exposed by the service
      targetPort: 80   # Port exposed by the Pod/container
  type: ClusterIP
