apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - protocol: TCP
    port: 80        # ClusterIP port (internal)
    targetPort: 80  # Pod port
    nodePort: 30080 # External access (range: 30000-32767)
